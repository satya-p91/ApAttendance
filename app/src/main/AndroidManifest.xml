<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="online.forgottenbit.attendance1">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".teacher.ReceiveStudentData"></activity>
        <activity android:name=".teacher.AddStudent" />
        <activity android:name=".teacher.BatchAndSubWiseAtten" />
        <activity
            android:name=".teacher.BatchActivity"
            android:parentActivityName=".teacher.TeacherDashboard" />
        <activity android:name=".teacher.TeacherRegistration" />
        <activity android:name=".teacher.TeacherDashboard" />
        <activity android:name=".StudentVerification" />
        <activity android:name=".StudentDashBoard" />
        <activity android:name=".StudentRegistration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>